<% memo_info = @dbcon.run_sql(DBConnection.choose_sql(:detail), [@params['id']]).first %>
<% @dbcon.run_sql(DBConnection.choose_sql(:delete), [@params['id']]) %>

次のメモを削除しました

<div class="content">
<p><%= sanitizing_text(memo_info['title']) %></p>
<p>
  <% memo_info['content'].split.each do |n| %>
    <%= sanitizing_text(n) %> <br>
  <% end %>
</p></div>

<a href="/">
  <button type="button">TOPへ</button>
</a>
